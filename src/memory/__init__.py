# Memory package — split into focused modules:
#   memory.py      — Core CRUD, search, recall, embeddings, loading
#   threads.py     — Thread management, rotation, extraction, summarization
#   maintenance.py — Stats, expiry, review queue, backfill
