# Generated by install.sh. from_scratch = you make up your @handle + Cove name in
# the setup wizard (checked for uniqueness across the network). No domain here —
# claim your address in the browser after setup.
from_scratch: true
team: on
deploy:
  target: standalone
  lucid_cove_path: /Users/mymac/Documents/LucidCove/OpenSource/lucid-cove
  app_port: 8200
  nextcloud_port: 8080
  matrix_port: 8008
  voice_port: 8301
  mesh_ip: 100.64.0.2
model_providers:
  - openrouter        # add your key in the wizard / Settings — not required to boot
matrix:
  enabled: true       # Connect/chat — fully works once you claim an address (needs matrix.{domain})
