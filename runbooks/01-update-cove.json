{
  "id": "update-cove",
  "num": 1,
  "title": "Update your Cove",
  "when": "A new version of Lucid Cove is out, or you pulled changes.",
  "category": "ops",
  "audience": "steward",
  "description": "Pull the latest Lucid Cove and rebuild your Cove. Database migrations apply automatically on restart. The paths below are filled in for your box.",
  "steps": [
    {
      "label": "Pull the latest code",
      "command": "cd {{CLONE_DIR}} && git pull",
      "note": "This is your lucid-cove clone (the folder you cloned from GitHub)."
    },
    {
      "label": "Rebuild + restart",
      "command": "cd {{COVE_DIR}} && docker compose up -d --build --force-recreate",
      "note": "This is your Cove folder (the one with docker-compose.yml). --force-recreate is REQUIRED: the app's code is bind-mounted from your clone and merged at container START, so without recreating, a pull won't take effect and migrations won't run. Migrations apply automatically on the fresh boot."
    }
  ]
}
